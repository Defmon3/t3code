diff --git a/dist/NodeChildProcessSpawner.js b/dist/NodeChildProcessSpawner.js
index 29b02311c..94550a078 100644
--- a/dist/NodeChildProcessSpawner.js
+++ b/dist/NodeChildProcessSpawner.js
@@ -239,7 +239,7 @@ const make = /*#__PURE__*/Effect.gen(function* () {
   };
   const spawn = (command, spawnOptions) => Effect.callback(resume => {
     const deferred = Deferred.makeUnsafe();
-    const handle = NodeChildProcess.spawn(command.command, command.args, spawnOptions);
+    const handle = NodeChildProcess.spawn(command.command, command.args, { ...spawnOptions, windowsHide: process.platform === "win32" && !spawnOptions.detached });
     handle.on("error", error => {
       resume(Effect.fail(toPlatformError("spawn", error, command)));
     });
@@ -256,7 +256,7 @@ const make = /*#__PURE__*/Effect.gen(function* () {
   const killProcessGroup = (command, childProcess, signal) => {
     if (globalThis.process.platform === "win32") {
       return Effect.callback(resume => {
-        NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, error => {
+        NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, { windowsHide: true }, error => {
           if (error) {
             resume(Effect.fail(toPlatformError("kill", toError(error), command)));
           } else {
@@ -274,9 +274,9 @@ const make = /*#__PURE__*/Effect.gen(function* () {
   };
   const killProcessGroupOnExit = (childProcess, signal) => {
     if (globalThis.process.platform === "win32") {
-      NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, () => {
-        // ignore errors during best-effort cleanup
-      });
+      try {
+        NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, { windowsHide: true }, () => {});
+      } catch {}
       return;
     }
     try {
diff --git a/src/NodeChildProcessSpawner.ts b/src/NodeChildProcessSpawner.ts
index bf6f007c4..bb89d9e13 100644
--- a/src/NodeChildProcessSpawner.ts
+++ b/src/NodeChildProcessSpawner.ts
@@ -331,7 +331,7 @@ const make = Effect.gen(function*() {
       const handle = NodeChildProcess.spawn(
         command.command,
         command.args,
-        spawnOptions
+        { ...spawnOptions, windowsHide: process.platform === "win32" && !spawnOptions.detached }
       )
       handle.on("error", (error) => {
         resume(Effect.fail(toPlatformError("spawn", error, command)))
@@ -354,7 +354,7 @@ const make = Effect.gen(function*() {
   ) => {
     if (globalThis.process.platform === "win32") {
       return Effect.callback<void, PlatformError.PlatformError>((resume) => {
-        NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, (error) => {
+        NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, { windowsHide: true }, (error) => {
           if (error) {
             resume(Effect.fail(toPlatformError("kill", toError(error), command)))
           } else {
@@ -376,9 +376,9 @@ const make = Effect.gen(function*() {
     signal: NodeJS.Signals
   ): void => {
     if (globalThis.process.platform === "win32") {
-      NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, () => {
-        // ignore errors during best-effort cleanup
-      })
+      try {
+        NodeChildProcess.exec(`taskkill /pid ${childProcess.pid} /T /F`, { windowsHide: true }, () => {})
+      } catch {}
       return
     }
     try {
